package com.slacks.dao;

import org.springframework.stereotype.Repository;

import com.slacks.common.dao.AbstractDAO;
import com.slacks.utils.FileDownload;

@Repository("fileDAO")
public class FileDAO extends AbstractDAO{
	public String findDirectory(FileBoardList fileBoardList) {
		fileBoardList = select("user.findDirectory", fileDownload);
		return directory;
	}
}
